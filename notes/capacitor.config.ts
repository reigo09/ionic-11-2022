import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.notes',
  appName: 'Notes',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
