import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reigo.ionic',
  appName: 'reigo-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
